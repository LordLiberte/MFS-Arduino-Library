#ifndef COREFSM_SHIM_BlockBase_H
#define COREFSM_SHIM_BlockBase_H
/* Puente de compatibilidad.
 * Los proyectos anteriores hacen #include <BlockBase.h>. El archivo real vive ahora
 * en core/, pero este puente mantiene esos sketches compilando sin tocarlos. */
#include "core/BlockBase.h"
#endif
