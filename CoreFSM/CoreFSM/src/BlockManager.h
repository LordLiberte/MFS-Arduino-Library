#ifndef COREFSM_SHIM_BlockManager_H
#define COREFSM_SHIM_BlockManager_H
/* Puente de compatibilidad.
 * Los proyectos anteriores hacen #include <BlockManager.h>. El archivo real vive ahora
 * en core/, pero este puente mantiene esos sketches compilando sin tocarlos. */
#include "core/BlockManager.h"
#endif
